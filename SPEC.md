# yahoo-auction-image-maker 仕様書

> このファイルはシステムの設計意図・業務ルール・帯レイアウト・プリセット定義の正式な定義です。
> コード変更時は必ずこの仕様書も同期更新してください。
> 最終更新: 2026-07-24（MVP実装。★暫定＝ユーザー承認前の既定値。目視調整して確定する）

## 1. システム概要

### 1.1 目的
16:9 の商品写真を、ヤフーオークション（ヤフオク）出品用の正方形（1:1）画像に加工する
ブラウザアプリを提供する。写真の上下に「帯」を自動生成して正方形化し、
商品情報をプリセット選択でバッジ表示する。各拠点のスタッフが使う。

### 1.2 対象範囲
- 対象: 16:9 の商品写真の正方形化 + 帯生成 + プリセットバッジ表示 + 高解像度ダウンロード
- 対象外（現時点）: 画像の色補正・トリミング編集・複数枚一括処理・クラウド保存
  （必要になれば「未解決課題」で検討）

### 1.3 基本方針
- **無料運用**: ブラウザ内（Canvas）で完結。外部送信なし。公開は GitHub Pages。
- **高解像度・クリアな画質（必須）**: 元写真の実ピクセルで描画し、PNG 出力。劣化させない。
- **プリセット選択のみ**: スタッフの入力ミスを最小化。手入力は最小限。
- 全ての比較・計算の基準を明記
- 不明点は推測せずユーザーに確認

---

## 2. データモデル

### 2.1 入力
- 元画像: 16:9 のラスター画像（JPEG/PNG）。ファイル選択 or ドラッグ&ドロップで読み込む。
- 選択されたプリセット群（上帯・下帯バッジ）

### 2.2 出力
- 正方形（1:1）の PNG 画像。ファイル名は日時 or 商品名ベース（要検討）。
- 端末にダウンロード。

### 2.3 想定レイアウト（初期案 / 独自開発、参考画像はあくまで参考）
```
┌─────────────────────────────┐
│  上帯：キャッチコピー + ロゴ     │  ← 高さは独自設定
├─────────────────────────────┤
│                             │
│      16:9 の元写真           │  ← アスペクト比を保持して中央配置
│                             │
├─────────────────────────────┤
│  下帯：商品情報バッジ横並び      │  ← 高さは独自設定
└─────────────────────────────┘
   全体が 1:1（正方形）になるよう上下帯の高さを計算
```
※ 帯のサイズ・マーク・配色は独自開発する（参考画像の模倣ではない）。

---

## 3. 業務ルール

- [開発しながら追記]
- 上帯・下帯を付けても写真本体のアスペクト比（16:9）は歪めない
- バッジは選択された数に応じて自動レイアウト（等間隔・折返し等は要検討）

---

## 4. 計算ロジック（正方形化）

- 出力を 1 辺 `S` の正方形とする。**`S` = 元画像の実幅**（表示縮小せず高解像度維持）。
- 写真部: 幅 `S`、高さ `S * 9/16 = 0.5625 S`。
- 帯配分（★暫定・renderer.js `LAYOUT`）:
  - 上帯 `TOP_BAND_RATIO = 0.10 S`（ロゴ帯。細く。参考画像準拠で縮小）
  - 下帯 `BOTTOM_BAND_RATIO = 0.2475 S`
  - 写真部 = 残り `1 - 0.10 - 0.2475 = 0.6525 S`（cover 描画。端数を吸収し合計を厳密に `S`）。
  - 上帯を細くした分は写真部へ回す方針（下帯バッジは維持）。
- 元画像が 16:9 でない場合: ★暫定で **中央基準の切り抜き（cover）**。歪めない。
  ずれが大きい場合はコンソール警告のみ（renderer.js `drawPhotoCover` / main.js）。
- 実装: `js/renderer.js` の `LAYOUT` 定数・`renderSquare`。

---

## 5. UI 仕様

- 画像アップロード（ボタン + ドラッグ&ドロップ）
- 上帯プリセット選択（キャッチコピー・ロゴ 等）
- 下帯バッジプリセット選択（美品／バリ溝／夏タイヤSET／商品ランク／本数 等をチェック/選択）
- プレビュー表示（生成結果をその場で確認）
- 「ダウンロード」ボタン（高解像度 PNG）
- スタッフが迷わないよう、ボタンは大きく手順は最小に
- [詳細は開発しながら追記]

---

## 6. プリセット定義（バッジ帯の項目）

★暫定実装（`js/presets.js`）。アイコンは**絵文字**（無料・オフライン・素材不要）を採用。

### 上帯（ロゴ帯・単一選択）
- **ロゴ＋ジムニーリユースパーツ**（黄背景）: 左に C.L.LINK ロゴ、右に「ジムニーリユースパーツ」黒文字
- 上帯なし
- ロゴ素材: `assets/logo.png`（C.L.LINK CUSTOM PARTS ロゴPDFを高解像度PNG化）
- ロゴは描画時に輝度→アルファ変換で白背景を透過し、余白をトリミングして黄色帯に直接配置（renderer.js `getProcessedLogo`）

### 下帯バッジ（項目ごとに選択・選択漏れ防止UI）
項目（`js/presets.js` `BADGE_CATEGORIES`）:
- 状態: 中古 / 未使用（択一）
- ランク: A / B / C（択一）
- 保証付: **全商品強制・常時ON**（非トグル。UIは「ON固定」表示）
- 適合型式: JA11/JB31/JA22/JB32/JB23/JB33/JB43/JB64/JB74/JC74（**複数・最大4**、選択型式を**1バッジに集約**）
- タイヤの種類: サマー / スタッドレス（択一）
- その他: 美品 / バリ溝（各トグル）

バッジ表示仕様（`js/renderer.js` `drawBottomBand` / `drawBadgeSquare`）:
- **角丸の正方形・固定サイズ**（5個基準で辺長を算出）。マークは使わず**テキストのみ**。
- 文字サイズの強弱でデザイン（例: ランクは「A」を大、上に小さく「ランク」）。
- 帯背景はグレー #f2f2f2。**バッジはカテゴリ別ビビッドカラー＋光沢グラデーション**（★暫定）。
  - 参考画像（フラット単色・美品=紫/バリ溝=紫/夏タイヤ=橙/ランク=銀/本数=紺）との類似クレーム回避のため、**配色を変え、質感を光沢仕上げで差別化**する。
  - 配色（`js/presets.js` `BADGE_COLORS`）: 状態=ティール #0f9d8f / ランク=クリムゾン #c0392b / タイヤ=ブルー #2563c9 / 適合型式=インディゴ #5b4bc4 / 保証付=グリーン #1f9a4d / 美品=マゼンタ #c2317f / バリ溝=アンバー #e08a1e。
  - 描画: ベース色の縦グラデ（上=明30% / 中=ベース / 下=暗38%）＋上半分のグロスハイライト（白28%→透明）＋内側の明るい縁取り（白42%）＋白文字に軽い影。`drawBadgeSquare(accent)` / `shadeColor`。
- **最大5個**。5個未満は全体を**センター寄せ**（左右に均等の空白）。5個超は自動縮小して全数を収める。
- 表示順（左→右）: 状態 → ランク → タイヤ → 適合型式 → 保証付 → 美品 → バリ溝。

---

## 7. 除外・例外ルール
- [開発しながら追記]

---

## 8. 未解決課題

- [~] 上帯・下帯の高さ配分比 → ★暫定 上0.19 / 下0.2475。目視で承認 or 調整。
- [~] プリセット項目の正式リスト → ★暫定実装済。文言・値の選択肢を承認 or 追加。
- [x] バッジのアイコン素材 → 絵文字を採用（追加素材不要・無料原則に合致）。
- [ ] 会社ロゴ画像の扱い（内蔵 / アップロード）→ 現状ロゴ未実装。要決定。
- [~] 出力ファイル名 → ★暫定 `yahoo-auction-YYYYMMDDhhmmss.png`。商品名ベースにするか要決定。
- [~] 16:9 でない場合の扱い → ★暫定 中央 cover 切り抜き＋警告。letterbox 案も検討可。
- [ ] GitHub Pages での公開手順・URL を確定する

★暫定値は目視確認のうえ、ユーザー承認で確定する。
