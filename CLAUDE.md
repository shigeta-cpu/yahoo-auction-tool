# yahoo-auction-image-maker

16:9 の写真を、ヤフオク用の正方形（1:1）画像に加工するブラウザアプリを開発するプロジェクト。
写真の上下に「帯（キャッチコピー帯 / 商品情報バッジ帯）」を自動生成し、正方形化する。
各拠点のスタッフが使う。**無料運用・高解像度出力**が絶対条件。

---

## プロジェクトの目的（概要）

- **入力**: 16:9 の商品写真
- **処理**: 上下に帯を追加して 1:1（正方形）に加工
- **上帯**: キャッチコピー帯（例「パーツ販売ナンバーワン」＋ロゴ）
- **下帯**: 商品情報バッジ帯（美品／バリ溝／夏タイヤSET／商品ランク／本数 等をプリセットから選択）
- **入力方式**: プリセット選択のみ（スタッフの入力ミスを最小化）
- **出力**: 端末にダウンロード（高解像度 PNG）
- **技術**: ブラウザ完結（HTML + JS + Canvas、GitHub Pages で無料公開）
- **利用者**: 各拠点スタッフ（IT に詳しくない人でも使える簡単 UI）

詳細は必ず SPEC.md を参照すること。

---

## 司令塔ルール（自動ルーティング）

ユーザーの指示を受け取り、指示内容から最適なエージェントを自動判断して起動する。

| ユーザーの指示パターン | 起動するエージェント | タイミング |
|----------------------|-------------------|-----------|
| 画像処理・Canvas描画・帯レイアウトの開発/修正 | pre-analyst | コードを書く前 |
| コード変更・リファクタリング完了 | regression-checker | コード変更後 |
| 帯の見た目・プリセット項目・業務ルールの確認 | domain-advisor | 実装結果が出た後 |
| 大規模な機能追加・設計変更 | /ultraplan → 設計確定後に実装 | 実装前 |

自動起動の判断基準:
- 「〜を実装して」「〜を作って」→ 対象画像/レイアウト仕様があれば pre-analyst を先に起動
- 「〜を修正して」「〜を直して」→ 修正後に regression-checker を自動起動
- 「画質が悪い」「レイアウトが崩れる」「文字がはみ出る」→ domain-advisor を起動
- 判断に迷う場合 → ユーザーに確認してから起動

---

## 作業フロー

通常の開発フロー:
1. ユーザーからの依頼を受ける
2. SPEC.md を読んで現在の仕様を把握
3. 指示内容から最適なエージェントを自動選択
4. [事前分析が必要] → pre-analyst で対象画像/レイアウト/Canvas処理を分析
5. 分析結果 + Skills 参照のもとにコードを実装
6. regression-checker で既存機能への影響を検証（自動）
7. [帯の見た目・業務ルール関連] → domain-advisor で検証
8. SPEC.md を更新（コードと必ず同期）
9. ブラウザで実際に画像を生成して動作確認（画質・レイアウトを目視）

新セッション開始時（最重要）:
1. SPEC.md を必ず読む
2. error-catalog を読む（過去のバグを把握）
3. 前回の未解決課題を確認
4. 変更箇所の影響範囲を把握してから着手

---

## Skills（共通マニュアル）定義

全エージェントが参照する共通知識ベース。
Skills は「成長するマニュアル」— 新しいルールや NG 例が見つかるたびに追記し、
使い込むほど品質が上がる。

| Skill | 内容 | 参照タイミング |
|-------|------|--------------|
| coding-standards | コーディング規約・命名規則・ファイル構成 | コード実装時 |
| error-catalog | 過去のバグと修正・再発防止策 | バグ修正時・実装時 |
| data-contracts | プリセット定義・帯レイアウト仕様・入出力契約 | プリセット/描画実装時 |

Skill の更新ルール:
- バグを修正したら → error-catalog に NG 例と修正内容を追記
- 新しいコーディングルールを決めたら → coding-standards に追記
- プリセット項目/帯サイズ仕様が変わったら → data-contracts を更新
- ユーザーに「これダメ」と言われたこと → 該当 Skill に即追記

---

## 絶対ルール（7原則）— 省略厳禁

### 1. 仕様書（SPEC.md）の同期更新（最優先）
- コード変更時は SPEC.md も必ず同期更新すること
- SPEC.md はシステムの設計意図・業務ルール・帯レイアウト・プリセット定義の正式な定義
- 新しいセッション開始時は必ず SPEC.md を読んでからコード修正に着手
- 不明点がある場合は推測で実装せずユーザーに確認
- 一度ユーザーが指定した修正は SPEC.md に記録し、再発を防止する
- バグ修正時は error-catalog にも追記（二重の再発防止）

### 2. 既存機能の保護（最優先）
- コード変更時は既存機能が破損しないことを必ずチェック
- 影響範囲が広い変更は事前に関連ファイルを全て把握してから着手
- 変更後は regression-checker を自動起動して検証
- 特に「画質（解像度・鮮明さ）」と「正方形化ロジック」は絶対に劣化させない

### 3. データ保護（最優先）
- 破壊的コマンドの実行前にユーザー確認を必ず取る
  - ファイル一括削除（rm -rf）
  - Git 履歴破壊（git reset --hard, git push --force）
- 大きな変更前にバックアップ（コミット）を取ること
- ユーザーがアップロードした元画像は加工前に必ず保持（劣化・上書きしない）

### 4. ハルシネーション（決めつけ）防止
- 結果を合わせることを重視しすぎて、根拠のない仮説を適用しない
- 新しい仮説を立てたら実際に画像を生成して目視検証してから提案する
- 新しいプリセット・帯デザインをユーザー承認なしに勝手に確定しない
- 不明点は推測せず必ずユーザーに確認する
- 一度ユーザーが指定した仕様は確実に維持する（再発防止）

### 5. 要件の積極的指摘
- 新しいプリセットや帯デザインを実装する際、必要な要素（アイコン・色・文言・サイズ）が揃っているか確認
- 不足があればユーザーに実装前に指摘する
- 「後で気づく」より「先に気づく」を徹底

### 6. Git 運用
- 大きな変更の前にバックアップ + コミット
- コミットメッセージは変更内容を正確に記述
- 破壊的な git コマンドはユーザー確認必須

### 7. 未解決課題の管理
- 未確定の要件や設計は SPEC.md の「未解決課題」セクションに記録
- 推測で実装せず、ユーザーに確認をとる

---

## このプロジェクト固有の絶対条件

- **無料運用**: 有料 API・有料ホスティング・課金サービスを導入しない。
  ブラウザ内処理（Canvas）で完結させ、外部送信しない。公開は GitHub Pages（無料）。
- **高解像度・クリアな画質**: 出力画像は元写真の解像度を最大限維持する。
  - Canvas は表示用サイズではなく元画像の実ピクセルで描画する
  - `imageSmoothingQuality = 'high'` を使う
  - PNG（可逆圧縮）で出力し、JPEG 劣化を避ける
  - 帯・文字も高 DPI でレンダリングし、ぼやけを防ぐ
- **スタッフが迷わない UI**: プリセット選択中心、ボタンは大きく、手順は最小に。
